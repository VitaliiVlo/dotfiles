# History
# Skip space-prefixed cmds — parity with zsh `setopt hist_ignore_space`
history_filter = [
  "^ ",
]
secrets_filter = true
strip_trailing_whitespace = true

# Search
search_mode = "fuzzy"
filter_mode = "global"
keymap_mode = "auto"
enter_accept = false
show_numeric_shortcuts = false

# Layout — Ctrl+R full-screen, Up arrow inline (20 lines)
inline_height = 0
inline_height_shell_up_key_binding = 20

# System (Brew owns updates)
update_check = false
