# macOS: ~/Library/Preferences/glow/glow.yml (glow ignores XDG on Darwin)
# Linux: ~/.config/glow/glow.yml

# Display
style: "auto"
showLineNumbers: true

# Behavior
pager: true
mouse: true
all: false
